fastapi>=0.110.0
uvicorn>=0.28.0
requests>=2.31.0
pandas>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Retrieval
pymilvus>=2.4.0
sentence-transformers>=3.0.0

# Local IBM Granite decoding. Required: the entropy engine consumes real
# per-token log-probabilities from the model, so there is no runtime without it.
# On Apple Silicon torch uses MPS automatically; no CUDA is involved.
torch>=2.2.0
transformers>=4.40.0
huggingface_hub>=0.23.0

# IBM Data Prep Kit — PII redaction. NOT installable here, and that is the point:
# dpk_pii_redactor's analyzer imports FlairRecognizer at module level, and flair
# pins transformers to 4.57.6. Installing it in this venv would downgrade the
# transformers that sentence-transformers embeds with. DPK is an offline INGEST
# dependency, not a serving one, so it gets its own environment:
#
#   python3.13 -m venv .venv-dpk
#   .venv-dpk/bin/pip install "data-prep-toolkit-transforms[pii_redactor]==1.1.8" \
#                             "pymilvus[milvus_lite]" sentence-transformers
#   .venv-dpk/bin/python phase2_ingestion_pipeline.py
#
# That run writes data/ingest_provenance.json, which is what /health reports.
# First execution downloads flair/ner-english-large (~1.5 GB). Without this venv
# the pipeline falls back to Presidio-minus-Flair, or to regex if that is absent
# too, and says so rather than claiming DPK.

# Tests
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
