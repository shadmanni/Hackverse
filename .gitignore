
*.ppt
*.pptx
*.pdf
.venv
__pycache__/

# DPK runs in its own venv (see dpk_ingest.py) — never install it here.
.venv-dpk/

# Runtime compliance trail, appended per interception.
data/audit_log.jsonl

# Same, aggregated: the gap queue is accumulated at runtime, so a committed copy
# would ship one machine's hit counts as if they were the product's.
data/knowledge_gaps.json
data/knowledge_gaps.json.tmp
